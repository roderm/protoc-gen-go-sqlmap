syntax = "proto3";

package testdata;

import "sqlmap/v1/sqlmap.proto";

option go_package = "testdata/badpb";

// Organisation claims to be a subtype, but Identity's oneof does not name it,
// so its discriminator value would fail Identity's CHECK.
message Identity {
  option (sqlmap.v1.table) = { name: "identity" };
  string id = 1 [(sqlmap.v1.col) = { fieldname: "id", pk: PK_MAN }];

  oneof type {
    option (sqlmap.v1.subtypes) = { discriminator: "kind" };
    Person person = 5;
  }
}

message Person {
  option (sqlmap.v1.table) = {
    name: "person"
    subtype_of: { entity: "Identity" }
  };
  string id = 1 [(sqlmap.v1.col) = { fieldname: "id", pk: PK_MAN }];
}

message Organisation {
  option (sqlmap.v1.table) = {
    name: "organisation"
    subtype_of: { entity: "Identity" }
  };
  string id = 1 [(sqlmap.v1.col) = { fieldname: "id", pk: PK_MAN }];
}
